*** Settings ***
Documentation       This configuration file define Default UI options
Resource            ../00_commonconfig.txt


*** Variables ***
${DEFAULT ALLOW ALL INITIATORS}     false
${DEFAULT GATEWAY GROUP}            Default
${DEFAULT POOL}                     Default
${DEFAULT SSH IP}                   @{PUBLICIP}[0]
${DEFAULT SSH PASSWORD}             ${PASSWORD}
${DEFAULT SSH USERNAME}             ${USERNAME}
${DEFAULT WEB IP}                   @{PUBLICIP}[0]
${DEFAULT WEB PASSWORD}             ${UIPASS}
${DEFAULT WEB USERNAME}             ${UIADMIN}
${DEFAULT VIRTUAL STORAGE}          Default
